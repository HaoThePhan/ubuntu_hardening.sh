# ubuntu_hardening.sh
remove ubuntu telemetry, close unnecessary port and install necessary maintenance.

for system-maintenance.sh
instruction:

sudo nano /usr/local/bin/system-maintenance.sh
after that
sudo chmod +x /usr/local/bin/system-maintenance.sh
